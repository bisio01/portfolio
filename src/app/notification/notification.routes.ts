import { Routes } from '@angular/router';
import { NotificationComponent } from './notification.component';

export const NotificationRoutes: Routes = [
  {
    path: 'notification',
    component: <any>NotificationComponent
  }
];
